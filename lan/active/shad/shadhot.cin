; SHADHOT - CHECK FOR HOT APPLICATIONS
#DO SHADGLOBAL:C

#LOOP_WITH I = 1 .. %MAX_APL
   #IF (APL'I':BTT == "LOCAL") AND (APL'I':BAS == "HOT") #THEN #BLOCK
      #IF SHADHOT:DOS(%I) <> 0 #THEN #SET SHADHOT:DOS(%I) = 0
      #SET SHADHOT:DRT(%I) = CLOCK
   #BLOCK_END
   #ELSE_IF SHADHOT:DOS(%I) == 0 #THEN #BLOCK
      #IF SHADHOT:DRT(%I) == 0 #THEN #BLOCK
         #SET SHADHOT:DOS(%I)=10
      #BLOCK_END         ; IF SHADHOT:DRT(%I) == 0
   #BLOCK_END
#LOOP_END

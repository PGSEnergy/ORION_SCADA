
; REMOVE COMMAND PROCEDURES

@NAME=""
@L=READ_TEXT ("SYSORF_CL.CPR",1,10000)
@LEN = LENGTH(%L)
#LOOP_WITH I=1..%LEN
   #IF SUBSTR(%L(%I),1,2)=="RF" #THEN #BLOCK
      @NAME=%L(%I)
      @COLPOS=LOCATE(%NAME, ":")
      @NAME=SUBSTR(%NAME, 1, %COLPOS-1)      
      @A=FETCH(0,"C", %NAME)
      #IF A:VIU<>-1 #THEN #DELETE 'NAME':C
   #BLOCK_END
#LOOP_END      
   
